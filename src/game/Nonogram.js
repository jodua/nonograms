class Nonogram {
    constructor(nonogram) {
        this.width = nonogram.width;
        this.height = nonogram.height;
        this.nonogramFields = nonogram.fields;
    }
}

export default Nonogram;